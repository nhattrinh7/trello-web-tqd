
import { createTheme} from '@mui/material/styles'

const theme =  createTheme({
  trello: {
    appBarHeight: '58px',
    boardBarHeight: '60px',
  },
  colorSchemes: {
    dark: true,
  },
}) 
export default theme

