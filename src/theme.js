
import { createTheme} from '@mui/material/styles'

const theme =  createTheme({
  trello: {
    appBarHeight: '48px',
    boardBarHeight: '58px',
  },
  colorSchemes: {
    dark: true,
  },
}) 
export default theme

